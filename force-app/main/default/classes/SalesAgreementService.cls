public with sharing class SalesAgreementService {
    public static SalesAgreementModel createSalesAgreement(Quote quote) {
        SalesAgreementModel agreementModel = new SalesAgreementModel();
        return agreementModel;
    }
}
